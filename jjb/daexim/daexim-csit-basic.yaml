---
- project:
    name: daexim-csit-basic
    jobs:
      - inttest-csit-1node

    # The project name
    project: 'daexim'

    # The functionality under test
    functionality: 'basic'

    # Project branches
    stream:
      - magnesium:
          branch: 'master'
          jre: 'openjdk11'
      - sodium:
          branch: 'stable/sodium'
      - neon:
          branch: 'stable/neon'

    install:
      - only:
          scope: 'only'

    # no mininet required
    tools_system_count: 0

    # Features to install
    install-features: 'odl-daexim-all,odl-netconf-topology,odl-jolokia,decanter-collector-jmx,decanter-appender-elasticsearch'

    # Robot custom options
    robot-options: ''
