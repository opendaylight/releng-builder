# REMOVE THIS LINE IF YOU WANT TO CUSTOMIZE ANYTHING BELOW
# Otherwise this file will be automatically overwritten by the template
# autobuilder.

# ODL Releng build templates
- project:
    name: openflowplugin
    jobs:
        - 'openflowplugin-sample-merge-{stream}'


    # stream:    release stream (eg. stable-lithium or beryllium)
    # branch:    git branch (eg. stable/lithium or master)
    stream:
        - beryllium:
            branch: 'master'
            jdk: openjdk7
            jdks:
                - openjdk7
                - openjdk8
        - stable-lithium:
            branch: 'stable/lithium'
            jdk: openjdk7
            jdks:
                - openjdk7
                - openjdk8
        - stable-helium:
            branch: 'stable/helium'
            jdk: openjdk7
            jdks:
                - openjdk7

    project: 'openflowplugin'

# For the Job templates below replace instances of:
# PROJECT_SHORTNAME with your project name (eg. circuitsw)
# PROJECT_PATH with your project name (eg. ofextensions/circuitsw)
# MAVEN_GOALS with your maven goals to build
# MAVEN_OPTS with your maven options to build

- job-template:
    name: 'openflowplugin-sample-merge-{stream}'

    # Job template for ODL merge jobs
    #
    # The purpose of this job template is to setup a ODL merge job
    # and deploy artifacts to Nexus.
    #
    # Required Variables:
    #     stream:    release stream (eg. stable-lithium or beryllium)
    #     branch:    git branch (eg. stable/lithium or master)

    project-type: maven
    node: dynamic_merge
    jdk: '{jdk}'

    logrotate:
        daysToKeep: '{build-days-to-keep}'
        numToKeep: '{build-num-to-keep}'
        artifactDaysToKeep: '{build-artifact-days-to-keep}'
        artifactNumToKeep: '{build-artifact-num-to-keep}'

    parameters:
        - project-parameter:
            project: '{project}'
        - integration-distribution-git-url

    scm:
        - integration-gerrit-scm:
            credentials-id: '{ssh-credentials}'
            basedir: 'openflowplugin'
            refspec: ''
            branch: '{branch}'
        - integration-distribution-scm:
            credentials-id: '{ssh-credentials}'
            branch: '{branch}'

    wrappers:
        - build-timeout
        - ssh-agent-credentials:
            users:
                - '{ssh-credentials}'

    triggers:
        - gerrit-trigger-patch-merged:
            name: 'openflowplugin'
            branch: '{branch}'

    prebuilders:
        - wipe-org-opendaylight-repo
        - jacoco-nojava-workaround
        - provide-maven-settings:
            global-settings-file: '{odl-global-settings}'
            settings-file: '{openflowplugin-settings}'
        - integration-get-bundle-url
        - inject:
            properties-file: 'bundle.txt'
        - integration-create-merge-pom

    maven:
        maven-name: '{mvn33}'
        root-pom: 'pom.xml'
        goals: '-Dmaven.repo.local=/tmp/r -Dorg.ops4j.pax.url.mvn.localRepository=/tmp/r clean install javadoc:aggregate -Dmerge -Dstream={stream}'
        maven-opts: '-Xmx1024m -XX:MaxPermSize=256m'
        settings: '{openflowplugin-settings}'
        global-settings: '{odl-global-settings}'
        post-step-run-condition: UNSTABLE

    postbuilders:
        - conditional-step:
            condition-kind: file-exists
            condition-filename: openflowplugin/deploy-site.xml
            condition-basedir: workspace

            # The strategy here is intentional to run Maven site:deploy twice
            # once using regular pom.xml to produce a staged-site which is
            # then used by deploy-site.xml to push to Nexus. This is a
            # workaround to Maven Site's default linking code which creates
            # incorrect URLs for sites due to auto-detection assuming your
            # project is configured in a certain way which ODL is not.
            steps:
            - maven-target:
                maven-version: '{mvn33}'
                pom: openflowplugin/pom.xml
                goals: 'site:deploy -Dstream={stream}'
                java-opts:
                    - '-Xmx2g'
                settings: '{openflowplugin-settings}'
                global-settings: '{odl-global-settings}'
            - maven-target:
                maven-version: '{mvn33}'
                pom: openflowplugin/deploy-site.xml
                goals: 'site:deploy -Dstream={stream}'
                java-opts:
                    - '-Xmx2g'
                settings: '{openflowplugin-settings}'
                global-settings: '{odl-global-settings}'
        - trigger-builds:
            - project: 'integration-distribution-deploy-{stream}'
              block: true
              predefined-parameters:
                  BUNDLEURL=$BUNDLEURL

    reporters:
        - findbugs

    publishers:
        - archive-artifacts:
            artifacts: 'openflowplugin/model/*/target/site/**'
        - email-notification:
            email-prefix: '[openflowplugin]'
        - maven-deploy:
            id: ''
            unique-version: true
            deploy-unstable: false
        - jacoco-report

