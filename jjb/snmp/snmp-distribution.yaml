- project:
    name: snmp-distribution
    jobs:
        - '{project}-distribution-{stream}'

    # The project name
    project: 'snmp'

    # The project branches
    stream:
        - master:
            branch: 'master'

    jdk: 'openjdk7'

