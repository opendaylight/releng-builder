- project:
    name: packaging
    jobs:
        - '{project-name}-verify-python-{stream}'

    stream:
        - master:
            branch: 'master'

    project: 'integration/packaging'
    project-name: 'packaging'
