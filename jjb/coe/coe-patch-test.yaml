---
- project:
    name: coe-patch-test
    jobs:
      - inttest-patch-test

    project: 'coe'

    stream:
      - neon:
          branch: 'master'
      - fluorine:
          branch: 'stable/fluorine'
      - oxygen:
          branch: 'stable/oxygen'

    jdk: 'openjdk8'

    feature:
      - coe:
          csit-list: >
              coe-csit-1node-container-networking-all-{stream}
