- project:
    name: bgpcep-csit-bgpclustering
    jobs:
        - '{project}-csit-3node-periodic-{functionality}-{install}-{stream}'

    # The project name
    project: 'bgpcep'

    # The functionality under test
    functionality: 'bgpclustering'

    # Project branches
    stream:
        - beryllium:
            branch: 'stable/beryllium'
        - stable-lithium:
            branch: 'stable/lithium'

    install:
        - only:
            scope: 'only'
        - all:
            scope: 'all'

    # Features to install
    install-features: 'odl-restconf,odl-clustering-test-app,odl-bgpcep-bgp-all,odl-bgpcep-bgp,odl-netconf-clustered-topology'

    # Robot custom options:
    robot-options: '-v COUNT:1000000'

    # Trigger jobs: (daily)
    schedule: 'H H * * *'
