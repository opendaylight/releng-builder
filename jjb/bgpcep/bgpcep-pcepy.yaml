---
- project:
    name: bgpcep-pcepy
    jobs:
      - '{project-name}-verify-python-{stream}'

    stream:
      # Pcepy was removed in Carbon.
      - boron:
          branch: 'stable/boron'

    toxdir: 'pcep/pcepy'
    project: 'bgpcep'
    project-name: 'bgpcep'
