---
- project:
    name: bgpcep-pcepy
    jobs:
      - '{project-name}-verify-python-{stream}'

    stream:
      # Pcepy was removed in Carbon.
      - oxygen:
          branch: master
      - nitrogen:
          branch: stable/nitrogen

    toxdir: 'pcep/pcepy'
    project: 'bgpcep'
    project-name: 'bgpcep'
