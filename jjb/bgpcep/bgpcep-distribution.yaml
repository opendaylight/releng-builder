- project:
    name: bgpcep-distribution
    jobs:
        - '{project}-distribution-{stream}'

    # The project name
    project: 'bgpcep'

    # The project branches
    stream:
        - master:
            branch: 'master'
        - stable-helium:
            branch: 'stable/helium'

    jdk: 'openjdk7'

