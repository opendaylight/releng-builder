- project:
    name: bgpcep-csit-userfeatures-post-lithium
    jobs:
        - '{project}-csit-1node-{functionality}-{install}-{stream}'
        - '{project}-csit-verify-1node-{functionality}'

    # The project name
    project: 'bgpcep'

    # The functionality under test
    functionality: 'userfeatures-{stream}'

    # Project branches
    stream:
        - boron:
            branch: 'master'
            jre: 'openjdk8'
        - beryllium:
            branch: 'stable/beryllium'
            jre: 'openjdk7'

    install:
        - only:
            scope: 'only'
        - all:
            scope: 'all'

    # Features to install
    install-features: 'odl-restconf,odl-bgpcep-pcep,odl-bgpcep-bgp,odl-netconf-connector-ssh'

    # Robot custom options
    robot-options: ''

    # Trigger jobs (upstream dependencies)
    # Netconf is there for run-time re-configuration.
    # AAA could also be a dependency, but probability of breakage is too low to trigger everytime.
    trigger-jobs: >
        odlparent-distribution-{stream},
        yangtools-distribution-{stream},
        controller-distribution-{stream},
        mdsal-distribution-{stream},
        netconf-distribution-{stream},
        tcpmd5-distribution-{stream},
        bgpcep-distribution-{stream}
