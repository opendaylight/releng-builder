- project:
    name: tsdr-distribution
    jobs:
        - '{project}-distribution-{stream}'

    # The project name
    project: 'tsdr'

    # The project branches
    stream:
        - master:
            branch: 'master'

    jdk: 'openjdk7'

