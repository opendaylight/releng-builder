---
- project:
    name: alto-csit-setup
    jobs:
      - inttest-csit-1node

    # The project name
    project: 'alto'

    # The functionality under test
    functionality: 'setup'

    disable-job: true

    # Project branches
    stream:
      - magnesium:
          branch: 'master'
          jre: 'openjdk11'
      - sodium:
          branch: 'stable/sodium'
      - neon:
          branch: 'stable/neon'

    install:
      - all:
          scope: 'all'

    # Features to install
    install-features: 'odl-alto-resourcepool,odl-restconf-all,odl-alto-basic,decanter-collector-jmx,decanter-appender-elasticsearch'

    # Robot custom options
    robot-options: ''

    # Don't spin up mininet vms as alto-csit-setup jobs don't need them
    tools_system_count: 0
