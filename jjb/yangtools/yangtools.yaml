---
- project:
    name: yangtools-master
    jobs:
      # The odl-maven-jobs-jdk21 job-group is expanded inline here so that
      # gerrit-maven-merge alone can be disabled. yangtools publishes master
      # SNAPSHOTs from GitHub Actions
      # (.github/workflows/gerrit-maven-merge.yaml) and running both CIs
      # double-publishes, advancing the SNAPSHOT buildNumber in
      # maven-metadata.xml from two places.
      # Re-collapse to the job-group if the GHA merge job is ever retired.
      - gerrit-maven-clm:
          nexus-iq-namespace: odl-
          nexus-iq-stage: release
          mvn-opts: "--add-opens java.base/java.util=ALL-UNNAMED"

      - gerrit-maven-merge:
          build-timeout: 180
          disable-job: true

      - gerrit-maven-stage:
          sbom-generator: true
          sign-artifacts: true
          cron: ""

      - gerrit-maven-mri-stage:
          sbom-generator: true
          sign-artifacts: true
          cron: ""
      - gerrit-release-merge:
          build-node: ubuntu2204-docker-2c-2g
          build-timeout: 90

    # Supplied by the odl-maven-jobs-jdk21 job-group; expanding it inline
    # above drops them, which silently falls back to the global defaults
    # (openjdk11 / mvn35).
    java-version: openjdk21
    mvn-version: mvn39

    project: yangtools
    project-name: yangtools
    stream: master
    branch: master
    build-node: ubuntu2204-docker-4c-4g

    mvn-settings: "yangtools-settings"

    dependencies: ""
    email-upstream: "[yangtools]"

    # Used by the release job
    staging-profile-id: 1271cf710cc09d

- project:
    name: yangtools-15.1.x
    jobs:
      - odl-maven-jobs-jdk21
      - gerrit-release-merge:
          build-node: ubuntu2204-docker-2c-2g
          build-timeout: 90

    project: yangtools
    project-name: yangtools
    stream: chromium
    branch: 15.1.x
    build-node: ubuntu2204-docker-4c-4g

    mvn-settings: "yangtools-settings"

    dependencies: ""
    email-upstream: "[yangtools]"

    # Used by the release job
    staging-profile-id: 1271cf710cc09d

- project:
    name: yangtools-14.0.x
    jobs:
      - odl-maven-jobs-jdk21
      - odl-maven-verify-jobs-jdk21
      - gerrit-release-merge:
          build-node: ubuntu2204-docker-2c-2g
          build-timeout: 90

    project: yangtools
    project-name: yangtools
    stream: titanium
    branch: 14.0.x
    build-node: ubuntu2204-docker-4c-4g

    mvn-settings: "yangtools-settings"

    dependencies: ""
    email-upstream: "[yangtools]"

    # Used by the release job
    staging-profile-id: 1271cf710cc09d

- project:
    name: yangtools-view
    views:
      - project-view
    project-name: yangtools
