- project:
    name: groupbasedpolicy-distribution
    jobs:
        - '{project}-distribution-{stream}'

    # The project name
    project: 'groupbasedpolicy'

    # The project branches
    stream:
        - master:
            branch: 'master'
        - stable-helium:
            branch: 'stable/helium'

    jdk: 'openjdk7'

