- project:
    name: groupbasedpolicy-csit-sxp
    jobs:
        - '{project}-csit-1node-{functionality}-{install}-{stream}'
   # The project name
    project: 'groupbasedpolicy'

    # The functionality under test
    functionality: 'sxp'

    # Project branches
    stream:
        - carbon:
            branch: 'master'
            jre: 'openjdk8'

    install:
        - only:
            scope: 'only'
        # - all:
        #     scope: 'all'

    # Mininet configuration (TOOLS)
    mininet-vms: 2

    # Features to install
    install-features: 'odl-restconf,odl-sxp-controller,odl-groupbasedpolicy-sxp-ep-provider,odl-groupbasedpolicy-sxp-ise-adapter,odl-groupbasedpolicy-ne-location-provider,odl-groupbasedpolicy-ios-xe,odl-sfc-ios-xe-renderer,odl-sxp-controller'

    # Robot custom options
    robot-options: ''

    # Trigger jobs (upstream dependencies)
    trigger-jobs: 'sfc-distribution-{stream},groupbasedpolicy-distribution-{stream},sxp-distribution-{stream}'
