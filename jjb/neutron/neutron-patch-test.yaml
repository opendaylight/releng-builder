---
- project:
    name: neutron-patch-test
    jobs:
      - inttest-patch-test

    project: "neutron"

    stream:
      - master:
          branch: "master"
          mvn-version: "mvn35"

    os-branch: "queens"

    jdk: "openjdk8"

    feature:
      - genius:
          csit-list: >
            genius-csit-1node-gate-only-{stream}
