- project:
    name: aaa-distribution
    jobs:
        - '{project}-distribution-{stream}'

    # The project name
    project: 'aaa'

    # The project branches
    stream:
        - master:
            branch: 'master'
        - stable-helium:
            branch: 'stable/helium'

    jdk: 'openjdk7'

