- project:
    name: vtn-distribution
    jobs:
        - '{project}-distribution-{stream}'

    # The project name
    project: 'vtn'

    # The project branches
    stream:
        - master:
            branch: 'master'
        - stable-helium:
            branch: 'stable/helium'

    jdk: 'openjdk7'

