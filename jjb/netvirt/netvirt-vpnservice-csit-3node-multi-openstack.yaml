- project:
    name: netvirt-vpnservice-3node-csit-openstack-integration
    jobs:
        - '{project}-csit-3node-openstack-{openstack}-{functionality}-{stream}'

    # The project name
    project: 'netvirt-vpnservice'

    # The project name
    functionality: 'openstack'

    stream:
        - boron:
            branch: 'master'
            jre: 'openjdk8'
        - beryllium:
            branch: 'stable/beryllium'
            jre: 'openjdk7'

    openstack:
        - mitaka:
            openstack-branch: 'stable/mitaka'
            odl-ml2-version:  'stable/mitaka'
        - liberty:
            openstack-branch: 'stable/liberty'
            odl-ml2-version:  'stable/liberty'

    openstack-vms: 4

    install-features: 'odl-ovsdb-openstack'

    schedule: 'H H * * *'

    odl-enable-l3: 'no'

    enable-haproxy: 'yes'

    enable-openstack-services: 'q-svc,q-meta,n-cauth,tempest'

    disable-openstack-services: 'swift,cinder,n-net,q-vpn'

    tenant-network-type: 'vxlan'

    ovs-create-bridges: 'br-int,br-ex'
