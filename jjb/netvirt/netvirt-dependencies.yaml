---
- project:
    name: netvirt-dependencies-centos
    jobs:
      - 'netvirt-quagga-centos-{stream}'

    # todo: remove 'ovs-scm' after boron EOL
    stream:
      - oxygen:
          branch: 'master'
          quagga-version: '4'
      - nitrogen:
          branch: 'stable/nitrogen'
          quagga-version: '4'
      - carbon:
          branch: 'stable/carbon'
          quagga-version: '2'
      - boron:
          branch: 'stable/boron'
          quagga-version: '1'

    project: 'netvirt'

    mvn-opts: ''
    mvn-params: ''
    mvn-version: mvn33

    # common parameters required for 'lf-infra-deploy-maven-file' builder
    repo-id: 'thirdparty'
    upload-files-dir: '$WORKSPACE/upload_files'

- job-template:
    name: 'netvirt-quagga-centos-{stream}'

    project-type: freestyle
    node: centos7-java-builder-2c-8g
    concurrent: true

    group-id: 'quagga'

    properties:
      - opendaylight-infra-properties:
          build-days-to-keep: '{build-days-to-keep}'

    parameters:
      - opendaylight-infra-parameters:
          os-cloud: '{os-cloud}'
          project: '{project}'
          branch: '{branch}'
          refspec: 'refs/heads/{branch}'
          artifacts: '{archive-artifacts}'
      - lf-infra-maven-parameters:
          mvn-opts: '{mvn-opts}'
          mvn-params: '{mvn-params}'
          mvn-version: '{mvn-version}'
          staging-profile-id: ''
      - string:
          name: QUAGGA_VERSION
          # yamllint disable-line rule:line-length
          default: '{quagga-version}'
          description: 'quagga version'

    scm:
      - git-netvirt-quagga

    wrappers:
      - opendaylight-infra-wrappers:
          build-timeout: '{build-timeout}'

    builders:
      - netvirt-build-quagga
      - lf-infra-deploy-maven-file:
          global-settings-file: 'global-settings'
          settings-file: 'netvirt-settings'
          mvn-version: '{mvn-version}'
          repo-id: '{repo-id}'
          group-id: '{group-id}'
          upload-files-dir: '{upload-files-dir}'
          maven-repo-url: '$NEXUS_URL/content/repositories/thirdparty'

    publishers:
      - email-notification:
          email-recipients: '{email-recipients}'
          email-prefix: '[opflex]'
      - lf-infra-publish

- project:
    name: netvirt-dependencies-ubuntu
    jobs:
      - 'netvirt-quagga-ubuntu-{stream}'

    # todo: remove 'ovs-scm' after boron EOL
    stream:
      - oxygen:
          branch: 'master'
          quagga-version: '4'
      - nitrogen:
          branch: 'stable/nitrogen'
          quagga-version: '4'
      - carbon:
          branch: 'stable/carbon'
          quagga-version: '2'
      - boron:
          branch: 'stable/boron'
          quagga-version: '1'

    project: 'netvirt'

    mvn-opts: ''
    mvn-params: ''
    mvn-version: mvn33

    # Use the System version of Java on for ubuntu 16.04 nodes to avoid
    # JAVA_HOME getting set to default path which fails while running maven
    # commands.
    java-version: ''

    # common parameters required for 'lf-infra-deploy-maven-file' builder
    repo-id: 'thirdparty'
    upload-files-dir: '$WORKSPACE/upload_files'

- job-template:
    name: 'netvirt-quagga-ubuntu-{stream}'

    project-type: freestyle
    node: ubuntu1604-mininet-ovs-25-2c-4g
    concurrent: true

    group-id: 'quagga'

    properties:
      - opendaylight-infra-properties:
          build-days-to-keep: '{build-days-to-keep}'

    parameters:
      - opendaylight-infra-parameters:
          os-cloud: '{os-cloud}'
          project: '{project}'
          branch: '{branch}'
          refspec: 'refs/heads/{branch}'
          artifacts: '{archive-artifacts}'
      - lf-infra-maven-parameters:
          mvn-opts: '{mvn-opts}'
          mvn-params: '{mvn-params}'
          mvn-version: '{mvn-version}'
          staging-profile-id: ''
      - string:
          name: QUAGGA_VERSION
          # yamllint disable-line rule:line-length
          default: '{quagga-version}'
          description: 'quagga version'

    scm:
      - git-netvirt-quagga

    wrappers:
      - opendaylight-infra-wrappers:
          build-timeout: '{build-timeout}'

    builders:
      - netvirt-build-quagga
      - lf-infra-deploy-maven-file:
          global-settings-file: 'global-settings'
          settings-file: 'netvirt-settings'
          mvn-version: '{mvn-version}'
          repo-id: '{repo-id}'
          group-id: '{group-id}'
          upload-files-dir: '{upload-files-dir}'
          maven-repo-url: '$NEXUS_URL/content/repositories/thirdparty'

    publishers:
      - email-notification:
          email-recipients: '{email-recipients}'
          email-prefix: '[opflex]'
      - lf-infra-publish


- builder:
    name: 'netvirt-build-quagga'
    builders:
      - shell: !include-raw-escape: quagga-build.sh

- scm:
    name: 'git-netvirt-quagga'
    scm:
      - git:
          url: https://github.com/6WIND/zrpcd.git
          basedir: 'zrpcd'
          branches:
            - 'master'
          wipe-workspace: true
