# Lithium build jobs

- project:
    name: autorelease
    jobs:
        - 'autorelease-lithium-worker'

    # stream:    branch with - in place of / (eg. stable-helium)
    # branch:    branch (eg. stable/helium)
    stream:
        - master:
            branch: 'master'

    project: 'autorelease'
    jdk: 'openjdk7'
    repo_list: 'odlparent yangtools controller aaa openflowjava openflowplugin l2switch tcpmd5 bgpcep sfc ovsdb groupbasedpolicy lispflowmapping snbi dlux ttp vtn integration opflex packetcable snmp4sdn sdninterfaceapp plugin2oc defense4all'

- job-template:
    name: 'autorelease-lithium-worker'

    project-type: maven
    node: dynamic_verify
    jdk: '{jdk}'

    logrotate:
        daysToKeep: '{build-days-to-keep}'
        numToKeep: '{build-num-to-keep}'
        artifactDaysToKeep: '{build-artifact-days-to-keep}'
        artifactNumToKeep: '{build-artifact-num-to-keep}'

    parameters:
        - project-parameter:
            project: '{project}'
        - autorelease-repository-list-parameter
            repo_list: '{repo_list}'

    wrappers:
        - autorelease-build-timeout
        - ssh-agent-credentials:
            user: '{ssh-credentials}'

    triggers:
        - timed: 'H 0 * * *'

    scm:
        - git-scm:
            credentials-id: '{ssh-credentials}'
            refspec: ''
            branch: '{branch}'

    maven:
        maven-name: '{mvn32}'
        root-pom: 'pom.xml'
        goals: 'clean install -Pintegrationtests,docs -Dmaven.repo.local=$WORKSPACE/.m2repo -Dorg.ops4j.pax.url.mvn.localRepository=$WORKSPACE/.m2repo'
        maven-opts: '-Xmx4096m -XX:MaxPermSize=1024m -Dmaven.compile.fork=true'
        settings: '{autorelease-settings}'
        global-settings: '{odl-global-settings}'


    publishers:
        - email-notification:
            email-prefix: '[autorelease]'
