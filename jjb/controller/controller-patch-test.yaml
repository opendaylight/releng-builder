---
- project:
    name: controller-patch-test
    jobs:
      - inttest-patch-test

    project: "controller"

    stream:
      - sulfur:
          branch: "master"
          java-version: "openjdk11"
      - phosphorus:
          branch: "stable/phosphorus"
          java-version: "openjdk11"
      - silicon:
          branch: "3.0.x"
          java-version: "openjdk11"

    jdk: "openjdk8"
