---
- project:
    name: gnmi-master
    jobs:
      - odl-maven-jobs-jdk21
      - odl-maven-verify-jobs-jdk21

    project: gnmi
    project-name: gnmi
    branch: master
    stream: master
    build-node: ubuntu2204-docker-4c-4g

    mvn-settings: "gnmi-settings"
    build-timeout: 60

    # Used by the release job
    staging-profile-id: dd4b7084afb552

- project:
    name: gnmi-1.0.x
    jobs:
      - odl-maven-jobs-jdk21
      - odl-maven-verify-jobs-jdk21

    project: gnmi
    project-name: gnmi
    branch: 1.0.x
    stream: 1.0.x
    build-node: ubuntu2204-docker-4c-4g

    mvn-settings: "gnmi-settings"
    build-timeout: 60

    # Used by the release job
    staging-profile-id: dd4b7084afb552

- project:
    name: gnmi-release-management
    jobs:
      - gerrit-release-merge

    project: gnmi
    project-name: gnmi

    build-node: centos7-builder-2c-1g
    mvn-settings: gnmi-settings
    # Needs to be rather long until we sort out the performance of
    # https://nexus.opendaylight.org/content/repositories/opendaylight.release/org/opendaylight/
    build-timeout: 120

- project:
    name: gnmi-sonar
    jobs:
      - odl-sonar-jobs-jdk21:
          mvn-params: >
            -Dodl.jacoco.aggregateFile=$WORKSPACE/target/jacoco.exec
            -Dskip.karaf.featureTest=true
          sonar-mvn-goal: >
            jacoco:report -Djacoco.dataFile=$WORKSPACE/target/jacoco.exec
            sonar:sonar

    project: "gnmi"
    project-name: "gnmi"
    branch: "master"
    build-node: ubuntu2204-docker-4c-4g

    mvn-settings: "gnmi-settings"
    mvn-goals: "clean deploy"
    mvn-opts: "-Dintegrationtests"

- project:
    name: gnmi-info
    jobs:
      - gerrit-info-yaml-verify
    project: gnmi
    project-name: gnmi
    branch: master
    build-node: ubuntu2204-builder-2c-1g

- project:
    name: gnmi-view
    views:
      - project-view
    project-name: gnmi
